package com.itheima.Groovy是轻量级Java

/*return语句几乎总是可选的（参见2.11.1节）。
尽管可以使用分号分隔语句，但它几乎总是可选的（参见2.11.6节）。
方法和类默认是公开（public）的。
?.操作符只有对象引用不为空时才会分派调用。
可以使用具名参数初始化JavaBean（参见2.2节）。
Groovy不强迫我们捕获自己不关心的异常，这些异常会被传递给代码的调用者。
静态方法内可以使用this来引用Class对象。在下面的例子中，learn()方法返回的是Class对象，所以可以使用链式调用。*/
class Feature {
}
